{
  "name": "content-blocks-gui-playwright",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0"
  }
}
